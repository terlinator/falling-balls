void update() - draws the screen

bool kbhit() - returns true if the keyboard has been hit, false otherwise

char getKey() - returns the last key pressed

void plotPixel(int x, int y, int r, int g, int b) - plots a single pixel at location (x,y) and color (r,g,b)

int getRow() - returns the current row position of the plotter

int getCol() - returns the current column position of the plotter

void initSound(string sound) - activates a sound when given the file name, must be a wav file

void quitSound(string sound) - turns off (deactivates) a sound

void playSound(string sound) - plays an activated sound

void Sleep(int ms) - pause program for ms milliseconds

bool getMouseClick(int& x, int& y) - returns true if the mouse was clicked and stores the cursor location in x and y

bool getMouseDown(int& x, int& y) - returns true if mouse button is down was clicked and stores the cursor location in x and y

bool getMouseUp(int& x, int& y) - returns true if mouse button is up was clicked and stores the cursor location in x and y

bool getMouseMotion(int& x, int& y)  - returns true if mouse is in motion and stores the cursor location in x and y

void getMouseLocation(int& x, int& y) -  stores the cursor location in x and y